return {
  {
    'akinsho/bufferline.nvim',
    version = "*",
    dependencies = 'nvim-tree/nvim-web-devicons',
    config = function ()
      require('bufferline').setup{
        options = {
          -- sort_by = function(buffer_a, buffer_b)
          --   return buffer_a.modified_at > buffer_b.modified_at
          -- end
        },
        highlights = {}
      }
    end
  }
}

